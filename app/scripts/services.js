(function(window, angular, undefined) {

'use strict';

// define the module
var services = angular.module('app.services', []);

// services.factory('Editor', ['$route', '$routeParams',
//     function($route, $routeParams) {

//         // settings
//         Editor.latestTimeout = 15000; // 15 seconds

//         // constructor
//         function Editor(settings) {
//         }

//         // enable editing
//         Editor.prototype.edit = function() {
//             this.edit_mode = true;
//         };

//         // return for angular factory
//         return Editor;
//     }
// ]);

})(window, window.angular);
